#!/usr/bin/env python

import nibabel
import sys

from container_demo import threshold


file_in = sys.argv[1]
threshold = int(sys.argv[2])
file_out = sys.argv[3]

image = nibabel.load(file_in)
nibabel.save(image, file_out)
